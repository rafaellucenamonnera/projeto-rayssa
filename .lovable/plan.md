Add `can_be_responsible: !!p?.can_be_responsible` to the user object returned by the GET branch of `supabase/functions/admin-create-user/index.ts`, so the admin UI receives the flag directly without the secondary `profiles` fetch fallback. No other logic changes; deploy the function after edit.